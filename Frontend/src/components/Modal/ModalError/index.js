import { useState } from "react";
import Modal from "..";

function ModalError({ onSubmitClick, onCloseClick }) {

    return (
        <Modal
            title={"Lỗi"}
        >

        </Modal>
    );
}
export default ModalError;